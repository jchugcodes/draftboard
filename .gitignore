node_modules/

# dist/ is intentionally committed — it is the deploy artifact (see README).

# generated ADP pulls — regenerate with `npm run adp`, they go stale fast
adp-*.csv

# nflverse downloads — large, re-fetchable from the nflverse-data releases
stats_player_week_*.csv
stats_team_week_*.csv
games.csv

# generated situation ratings — rebuild with `npm run situation`
situation-*.json

*.log
npm-debug.log*
.DS_Store
Thumbs.db
.meta.json

# API probe dumps
espn*.json

# baked datasets — regenerated by CI on every deploy
public/data/
